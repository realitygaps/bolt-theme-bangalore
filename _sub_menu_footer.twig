{% macro display_menu_item(item, loop) %}
<li class="index-{{ loop.index }}{% if loop.first %} first{% endif %}{% if loop.last %} last{% endif %}{% if item.submenu is defined %} has-dropdown{% endif %}{% if item|current %} active{% endif %} menu-item menu-item-type-custom menu-item-object-custom">
    <a href="{{ item.link }}" {% if item.title is defined %}title='{{ item.title|escape }}'{% endif %}
       class='{% if item.class is defined %}{{item.class}}{% endif %}'>
        {{item.label}}
    </a>
    {% if item.submenu is defined %}
        <ol>
        {% for submenu in item.submenu %}
                {{ _self.display_menu_item(submenu, loop) }}
        {% endfor %}
        </ol>
    {% endif %}
</li>
{% endmacro %}

{% for item in menu %}
    {% if item.label is defined %}
        {{ _self.display_menu_item(item, loop) }}
    {% endif %}
{% endfor %}
